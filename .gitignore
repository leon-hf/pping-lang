# Byte-compiled / optimized
__pycache__/
*.py[cod]
*$py.class
*.so

# Distribution / packaging
build/
dist/
*.egg-info/
*.egg
MANIFEST
*.whl
*.tar.gz

# Virtualenvs
.venv/
venv/
env/
ENV/

# Test / coverage
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# Claude Code local config — all of it stays out of git
.claude/

# Codex project-local handoff/secrets — machine-private, never committed
.codex/

# Other agent tool local config — machine-private, never committed
.agents/

# pping-lang runtime data
*.duckdb
*.duckdb.wal
.pping-lang/
/var/lib/pping-lang/

# logs
*.log
.gstack/

# Personal design notes — kept on disk for reference, never committed
_design-notes/
_scratch/
_autopilot-stress/

# Ad-hoc root-level debug/check scripts from working sessions — never committed
/check_*.py
/dump_events.py

# Deploy tooling for the runw GPU box — kept on disk for the deploy-runw skill,
# never committed (carries infra topology + tailnet addressing)
deploy/runw/
.kimi-code/skills/deploy-runw/
src/pping_lang/ui/_*.html
